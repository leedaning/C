#include <stdio.h>
#include <math.h>

int main(void){

    int num;
    float pi;
    num = 1024;
    pi = acos(-1);
    printf("The number is %d \n", num);
    printf("The pi is %f \n", pi);
    return 0;
}